# The agent core (src/, payment_tools.py) depends only on the standard library,
# so it can be imported, tested and replayed without installing anything.
# Everything below is for the dashboard, API and LLM surfaces.

streamlit>=1.28.0
plotly>=5.18.0
pandas>=2.0.0
pyyaml>=6.0.0
fastapi>=0.109.0
uvicorn>=0.27.0
google-genai
mcp
