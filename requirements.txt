# Config loading. Absent, the code falls back to the defaults in settings.py.
pyyaml>=6.0.0

# REST API
fastapi>=0.109.0
uvicorn>=0.27.0

# LLM surfaces: the incident advisor and the tool-calling path. Both import
# this lazily and degrade to "no commentary" if it is missing, so the agent
# runs without it.
google-genai
mcp
